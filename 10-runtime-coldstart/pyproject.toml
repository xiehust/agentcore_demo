[project]
name = "coldstart-benchmark"
version = "0.1.0"
description = "AgentCore Runtime vs Lambda MicroVMs cold-start ping-pong benchmark (image size x concurrency)"
requires-python = ">=3.11"
# boto3 >= 1.43.44 ships the `lambda-microvms` service model used by microvm_coldstart_test.py
dependencies = ["boto3>=1.43.44", "bedrock-agentcore>=0.1"]
