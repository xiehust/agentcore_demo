# AgentCore Runtime V2 冷启动测试报告

## TL;DR

**us-west-2 的 V2 测试覆盖三档镜像（约 501 / 1025 / 1951 MiB）和四档并发（1/5/10/50），300 次正式探测全部成功，无错误或限流。** 新 session 首次调用整体 **p50 为 2.25 秒、p90 为 3.09 秒，最大值为 5.19 秒**；各镜像与并发组合的 p50 为 **2.15–2.99 秒**，本次未观察到延迟随镜像大小明显递增。300 次同 session 的 warm 跟测全部成功，整体 **p50 为 107.5 ms**。这里测量的是首次调用的端到端响应时间，不是单独的底层 microVM 启动耗时。

**并发 200 补测（同日 05:41 UTC）**：另运行 600 个正式尝试，459 成功、141 次 HTTP 429，全部为 `New session creation rate exceeded`。成功样本的 p50 为 2.32–5.83 秒，不代表 200 个会话均成功启动。补测结果与原 300 样本分开，详见下方“并发 200 补充测试”。

## 结论（原 c=1/5/10/50 矩阵）

**V2 完整矩阵 300/300 成功，无首次调用错误或限流；300 次 warm 跟测也全部成功。首次调用整体 p50 为 2,250.3 ms，p90 为 3,085.2 ms，最大值 5,192.1 ms；warm 整体 p50 为 107.5 ms。** 3 个额外部署后 smoke 全部通过；303 个 session 均停止成功，3 个临时 Runtime 均已删除并确认不存在。

最重要的变化是分布发生了改变，而不是所有场景都变快：

- **高并发首次调用更快**：c=10 的 V2 p50 为 2.15–2.21 秒，相较历史 7.32–13.46 秒降低 **70.5%–84.0%**；c=50 的 V2 p50 为 2.19–2.99 秒，相较历史 8.15–13.49 秒降低 **63.4%–83.8%**。
- **低并发首次调用更慢**：c=1/5 的历史 p50 为 0.41–1.18 秒，本次为 2.15–2.48 秒，耗时约为历史的 **1.92–5.21 倍**。不能用高并发改善掩盖低并发退化。
- **本次没有复现历史上随镜像大小明显增加的首次调用中位数**：同一并发下 1gb / 2gb 并未比 500mb 系统性更慢；c=50 反而是 500mb 较高。这是单次观测，不证明镜像大小对 V2 永远无影响。
- **Warm 没有改善**：各单元格 p50 为 104.2–128.2 ms，高于历史 68.6–101.5 ms；整体 107.5 ms 对比历史 75.0 ms，约增加 **43.3%**。

运行窗口：**2026-09-11 01:45:49–01:58:35 UTC（北京时间 09:45:49–09:58:35）**；正式矩阵为 01:52:43–01:58:29 UTC，约 5 分 46 秒，其余时间用于部署、smoke、静置和清理。历史比较是跨日期的描述性对照，不是当天 V1/V2 随机 A/B 实验。

## 环境与测试方法

- 测试日期：2026-09-11（UTC）；区域：**us-west-2**；账号：`434444145045`。客户端 EC2 的 IMDS 区域查询同样返回 us-west-2。
- 使用私有 boto3 / botocore **1.43.87**，创建 3 个独立临时 Runtime，显式指定 `platformVersion="V2"`，并在 READY 后核验 `GetAgentRuntime` 回显。PUBLIC 网络、HTTP 协议、`idleRuntimeSessionTimeout=60`。
- 复用 `../10-runtime-coldstart/` 的 ping-pong 镜像及 `AgentCoreColdstartRole`，不调用 LLM；未重建镜像、修改角色权限或更新原 Runtime。
- 镜像标签与大小如下；单位统一使用 **MiB（2²⁰ 字节）**，不是十进制 MB。当前 ECR 大小与历史部署清单一致，digest 与本地已有镜像 RepoDigests 一致。历史逐请求记录未保存 digest，不能仅凭今天的标签证明所有历史请求使用的内容完全一致。

| 标签 | 未压缩 MiB | ECR 压缩 MiB | 本次 digest 前缀 |
|---|---:|---:|---|
| 500mb | 500.63 | 382.62 | `sha256:8fce75a892c7…` |
| 1gb | 1024.63 | 906.78 | `sha256:a2231b90c973…` |
| 2gb | 1950.63 | 1833.06 | `sha256:4f3ae6ceae8d…` |

### 测量口径

直接复用原 `coldstart_test.py` 的 `probe`、`run_cell`、客户端配置及统计函数，不复制其压测实现。本次适配器仅负责 V2 部署、隔离输出、保存完整响应以及校验 HTTP 200 / `pong` / 数值时间戳；这些额外操作位于原计时区间之外，但会略微影响每轮请求结束后的调度。

1. **新 session 首次调用 E2E（主指标）**：每个探针使用全新 40 字符 `runtimeSessionId`，计时从发起 `InvokeAgentRuntime` 到完整读取响应体。下文表格中的“首次调用”均指此指标，不等价于已确认发生底层 microVM 启动。
2. **Warm 跟测**：每次首次调用返回后，立即复用同一 session 再调用一次，随后调用 `StopRuntimeSession`。Warm 跟测没有独立同步屏障，可能与其他线程的首次调用重叠，不是单独的稳态吞吐测试。
3. 关闭 botocore 重试：`total_max_attempts=1`，读超时 300 秒、连接超时 30 秒，最大连接池 100。并发线程通过 `threading.Barrier` 同时释放。
4. 完整矩阵：500mb / 1gb / 2gb，各运行 c=1 的 10 轮、c=5 的 4 轮、c=10 的 2 轮、c=50 的 1 轮。每镜像 100 个新 session，共 **300 个矩阵样本**，轮间暂停 5 秒。
5. 三个 Runtime 分别做一次部署后 smoke（首次调用 + warm），不计入矩阵；全部 smoke 完成后统一静置 180 秒。之后按镜像 500mb → 1gb → 2gb、并发 1 → 5 → 10 → 50 顺序测试。READY 和部署耗时不计入首次调用 E2E。
6. 分位数沿用原脚本的线性插值算法；首次调用延迟只统计成功样本，失败次数另列。不同单元格不混用输出目录。

## V2 实测数据

### 完整矩阵

下表延迟单位均为 **ms**。每行首次调用和 warm 均全部成功，首次调用限流/其他错误均为 0；每行 session 均停止成功。

| 镜像 | 并发 | 成功 / 样本 | 首次 p50 | 首次 p90 | 首次 max | 首次 mean | Warm p50 |
|---|---:|---:|---:|---:|---:|---:|---:|
| 500mb | 1 | 10/10 | 2149.3 | 2744.5 | 2754.7 | 2256.5 | 128.2 |
| 500mb | 5 | 20/20 | 2280.8 | 3102.9 | 3111.8 | 2415.3 | 118.2 |
| 500mb | 10 | 20/20 | 2161.9 | 2570.8 | 2788.1 | 2264.2 | 108.6 |
| 500mb | 50 | 50/50 | 2985.3 | 3345.7 | 5192.1 | 2891.0 | 104.3 |
| 1gb | 1 | 10/10 | 2482.8 | 3481.2 | 3791.9 | 2723.9 | 106.3 |
| 1gb | 5 | 20/20 | 2150.8 | 2763.5 | 3288.2 | 2284.0 | 105.8 |
| 1gb | 10 | 20/20 | 2212.0 | 2714.3 | 2811.6 | 2286.6 | 107.8 |
| 1gb | 50 | 50/50 | 2197.9 | 2710.8 | 3613.7 | 2293.3 | 108.3 |
| 2gb | 1 | 10/10 | 2377.3 | 2834.8 | 3165.1 | 2471.8 | 116.5 |
| 2gb | 5 | 20/20 | 2220.0 | 2576.9 | 2713.6 | 2245.8 | 104.2 |
| 2gb | 10 | 20/20 | 2149.3 | 2674.5 | 2810.5 | 2208.2 | 105.4 |
| 2gb | 50 | 50/50 | 2191.1 | 2528.9 | 3226.1 | 2234.8 | 105.5 |

### 部署后首次调用（不计入矩阵）

| 镜像 | 首次调用 ms | Warm ms | 创建请求至观察到 READY 的时间 |
|---|---:|---:|---:|
| 500mb | 2235.4 | 160.2 | 184.8 s |
| 1gb | 2376.7 | 121.0 | 217.1 s |
| 2gb | 2261.8 | 126.0 | 229.4 s |

这里的 READY 时间包含轮询和依次检查带来的观测延迟，是客户端观察值，不是精确服务端部署耗时。三个 Runtime 均回显 `platformVersion="V2"`。没有把创建 Runtime 所需约 3–4 分钟混入 session 首次调用的 2 秒级指标。

## 并发 200 补充测试

### 结果：出现新 session 创建速率限流

补测时间为 **2026-09-11 05:34:06–05:41:47 UTC**；正式突发窗口为 05:41:04–05:41:41。三档镜像各一轮、200 个客户端线程经屏障释放，共 **600 个新 session 首调用尝试**。本次成功 **459/600（76.5%）**，限流 **141/600（23.5%）**，没有其他首调用错误或超时。459 个成功首调用均完成 warm 跟测。

延迟单位为 **ms**，仅统计成功首调用；失败请求不被替换，也不补跑。原 c=1/5/10/50 的 300 个样本和以下 600 个尝试是两次独立运行，不混合计算分位数。

| 镜像 | 并发尝试 | 成功 / 尝试 | 成功率 | HTTP 429 | 首次 p50 | 首次 p90 | 首次 p95 | 首次 p99 | 首次 max | Warm p50 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 500mb | 200 | 195/200 | 97.5% | 5 | 5826.5 | 6719.9 | 6898.0 | 7206.0 | 7562.8 | 103.0 |
| 1gb | 200 | 148/200 | 74.0% | 52 | 2694.6 | 3180.2 | 3228.0 | 3894.0 | 4308.1 | 105.8 |
| 2gb | 200 | 116/200 | 58.0% | 84 | 2323.4 | 2678.5 | 2794.4 | 3340.7 | 3547.3 | 107.3 |

全部 141 次错误均为 `ThrottlingException` / HTTP 429，服务端消息为 **`New session creation rate exceeded`**。本次只读查询得到当前账号、区域的配额如下，未提交任何配额变更：

| 配额 | 实际值 |
|---|---:|
| 新 Runtime session 创建速率（L-8EE2AEA2） | 25 TPS |
| Runtime 数据面 API 速率（L-46ED137C） | 1000 TPS |
| 账号活跃 session 数（L-3E5722B2） | 5000 |

[官方配额文档](https://docs.aws.amazon.com/bedrock-agentcore/latest/devguide/bedrock-agentcore-limits.html)区分新 session 创建速率、数据面 API 速率和活跃 session 数。该错误直接说明被拒绝请求触发了**新 session 创建速率限制**，不能据此说 V2 的并发容量只有 116 或 148。配额是账号共享的；突发额度、前一轮消耗和其他业务流量未被完全隔离。三个镜像固定顺序执行，不能把后两组更高的限流率因果归于镜像更大。

### 与原 V2 c=50 的描述性对比

| 镜像 | 原 c=50 成功 / 尝试 | 原 c=50 p50 | c=200 成功 / 尝试 | c=200 p50 | 成功样本 p50 变化 |
|---|---:|---:|---:|---:|---:|
| 500mb | 50/50 | 2985.3 | 195/200 | 5826.5 | +95.2% |
| 1gb | 50/50 | 2197.9 | 148/200 | 2694.6 | +22.6% |
| 2gb | 50/50 | 2191.1 | 116/200 | 2323.4 | +6.0% |

两次使用相同镜像 digest、相同区域，但不同 Runtime 和运行时刻，且 c=200 存在明显的成功样本筛选偏差。不能只看成功请求的 p50 就认定 2gb 扩展更好；限流失败应与延迟同时阅读。**失败等待也不可忽略**：500mb 的 5 次限流请求耗时 p50 为 4557.7 ms、最大 4635.1 ms，并非立即拒绝；1gb/2gb 限流耗时 p50 分别为 78.8/66.2 ms。失败耗时保存在 `api_events.jsonl`，未混入成功请求的延迟分位数。

### 并发口径与客户端限制

- 保留原 `bench.probe` 的首调用、同 session warm 和停止流程。新增适配器只运行 c=200，连接池增至 **400**，SDK `total_max_attempts=1`；1,065 次 Invoke 的响应/错误元数据均记录 `RetryAttempts=0`。
- 屏障等待上限 30 秒，线程启动失败会中止屏障；每轮等待上限 12 分钟。记录屏障释放后的计时起点、完整成功响应、失败类型/请求元数据和每次停止结果。
- “200 并发”是 **200 个客户端线程的一轮突发**，不保证 200 个 HTTP 请求在同一时刻到达服务端，更不保证 200 个成功活跃 microVM。

| 镜像 | 首调用计时起点跨度 | 客户端首调用计时区间最大重叠数 |
|---|---:|---:|
| 500mb | 2700.9 ms | 200 |
| 1gb | 252.4 ms | 155 |
| 2gb | 251.7 ms | 138 |

这些区间从 SDK 调用前计时至读取响应/返回错误，包含客户端处理，不是抓包得到的线上请求并发。Warm 和停止按每个 session 独立推进，会与其他线程尚未完成的首调用重叠，并非单独的稳态 warm 压测。第一轮 500mb 的起点跨度明显更大，说明客户端突发被拉长；目前没有 CPU/TLS profiling，不能指定原因。后两轮部分请求快速被拒，因而峰值重叠数低于 200。不能将 200 个线程等同于 200 个同时成功运行的 session。

### 部署、证据与清理

- 新建三个临时 V2 Runtime，均 READY 并回显 `platformVersion=V2`。ECR digest/大小与原 V2 测试一致，本次**按 digest URI 部署**，不移动标签，不修改原 Runtime、IAM 或配额。
- 每个 Runtime 先做一次 smoke（首调用 + warm + 停止），三次全部成功；随后静置 180 秒。按 500mb、1gb、2gb 顺序执行突发，组间等 10 秒。生命周期 idle 60 秒、maxLifetime 600 秒；该 maxLifetime 是此次额外的安全上限。
- 正式尝试及 smoke 共 **603 个唯一 session ID**。459 个正式成功会话加 3 个 smoke，合计 **462 次 Stop 返回 HTTP 200**。141 个限流 session 均尝试停止，返回 `ResourceNotFoundException`（未找到或已终止）；这些不能写成停止成功，也不能证明它们从未短暂存在。
- 三个临时 Runtime 已删除，05:42 UTC 再次只读查询均确认 `ResourceNotFoundException`。原镜像、IAM、原 Runtime 和此前证据保留。自动生成的日志未删除，可能继续产生存储费用。
- 运行器退出码为 **1**：它将非 HTTP 200 的停止结果计为失败。`measurement_complete=true`、`cleanup_complete=true`、`all_invocations_successful=false` 原样保留；没有为了得到绿色结果重新发起任何调用。独立 verifier 区分“实验记录完整”与“所有请求成功”，只允许首调用限流后的已知不存在停止结果，其他停止错误仍拒绝。
- 9 项离线测试通过，覆盖实际 200 线程 mock 突发、限流、超时、错误响应、warm/stop 失败、线程启动失败及部分部署清理。独立验证器重算三组统计并核对全量 **1,668 条 API 事件**（1,065 Invoke + 603 Stop），源码 hash、603 个唯一 ID、V2/镜像配置与删除记录均通过。

独立只读复核重新计算全部统计及 API 事件对应关系，结果一致。另以只读注入方式验证：独立 verifier 会拒绝停止返回 AccessDenied、重复请求索引或被改动的延迟值；允许已知限流不等于放宽其他完整性条件。

原始数据：[`coldstart_v2_c200_2026-09-11/`](coldstart_v2_c200_2026-09-11/)。新增文件为 `coldstart_v2_c200.py`、`test_coldstart_v2_c200.py` 和 `verify_coldstart_v2_c200.py`；原脚本及原始证据未修改，旧 verifier 仍通过。

```bash
# 只读本地核验，包含已记录的限流，不重新调用 AWS
.venv/bin/python -B verify_coldstart_v2_c200.py results/coldstart_v2_c200_2026-09-11
.venv/bin/python -B -m unittest test_coldstart_v2_c200 -v

# 另一次明确获准的实测：创建三个临时 Runtime，会产生费用，目录必须不存在
.venv/bin/python -B -u coldstart_v2_c200.py --out results/coldstart_v2_c200_NEW_RUN
```

**补测结论：c=200 突发已触及账号的新 session 创建速率限制。** 成功请求仍为秒级，但不能从本次结果证明 200 个新 session 都能无损启动。若要测“200 个全部被接纳的首调用分布”，需另行批准提高相应配额，或明确改为分批创建的不同实验；本次不自动更改测试目标或云配额。新 session 的首次 E2E 仍不等于平台确认的底层冷启动。

## V1 与 V2 测试结果对比

V1 结果来自 [`10-runtime-coldstart/results/REPORT.zh.md`](../../10-runtime-coldstart/results/REPORT.zh.md)（**2026-07-09，us-west-2**），V2 为本报告的 **2026-09-11，us-west-2** 实测。两次均使用三档 ping-pong 镜像、四档并发及每镜像 100 次探测，不调用 LLM。

下文将原报告数据标为 **V1（历史）**：原部署脚本未显式指定 `platformVersion`，旧记录也没有该字段回显，不能把它当作本次重新核验过版本的控制组。原报告中的首次调用延迟取整到毫秒；以下从其配套 `summary.json` 取一位小数，并已用原始记录独立重算核对。

### 新 session 首次调用：p50 / p90 / max（ms）

两侧均统计**全部成功新 session 的首次调用 E2E**，不将 V2 全体与 V1 的“真实启动”子集混比。最后一列仅比较 p50，按 `(V2 / V1 - 1) × 100%` 计算；max 不包含失败请求的等待时间。

| 镜像 | 并发 | V1（历史）p50 / p90 / max | V2 p50 / p90 / max | p50 相对 V1 变化 |
|---|---:|---|---|---|
| 500mb | 1 | 412.2 / 436.0 / 503.0 | 2149.3 / 2744.5 / 2754.7 | 增加 421.4% |
| 500mb | 5 | 667.0 / 7339.4 / 7431.6 | 2280.8 / 3102.9 / 3111.8 | 增加 241.9% |
| 500mb | 10 | 7319.6 / 7590.8 / 7723.3 | 2161.9 / 2570.8 / 2788.1 | 降低 70.5% |
| 500mb | 50 | 8148.7 / 8348.3 / 10285.2 | 2985.3 / 3345.7 / 5192.1 | 降低 63.4% |
| 1gb | 1 | 732.4 / 1169.9 / 1208.4 | 2482.8 / 3481.2 / 3791.9 | 增加 239.0% |
| 1gb | 5 | 762.7 / 11437.8 / 15409.0 | 2150.8 / 2763.5 / 3288.2 | 增加 182.0% |
| 1gb | 10 | 11400.6 / 11470.1 / 11503.1 | 2212.0 / 2714.3 / 2811.6 | 降低 80.6% |
| 1gb | 50 | 11421.9 / 11501.3 / 39581.2 | 2197.9 / 2710.8 / 3613.7 | 降低 80.8% |
| 2gb | 1 | 1176.4 / 1811.2 / 2089.8 | 2377.3 / 2834.8 / 3165.1 | 增加 102.1% |
| 2gb | 5 | 1155.0 / 13477.5 / 13618.6 | 2220.0 / 2576.9 / 2713.6 | 增加 92.2% |
| 2gb | 10 | 13457.8 / 13609.5 / 15620.3 | 2149.3 / 2674.5 / 2810.5 | 降低 84.0% |
| 2gb | 50 | 13493.0 / 13633.8 / 15778.4 | 2191.1 / 2528.9 / 3226.1 | 降低 83.8% |

c=5 尤其说明只看中位数不够：V2 的 p50 虽然升高，但三档镜像的 p90 从 V1 的 7,339.4 / 11,437.8 / 13,477.5 ms 降到 3,102.9 / 2,763.5 / 2,576.9 ms。V1 低延迟与长尾混合的分布，在本次 V2 测试中更集中于 2–3 秒附近。

### Warm 与成功率

Warm 为同 session 的第二次调用，单位 **ms**；成功/样本列对应新 session 首次调用。V1 的 298 次、V2 的 300 次成功首次调用均有成功的 warm 跟测。

| 镜像 | 并发 | V1 Warm p50 | V2 Warm p50 | V1 成功 / 样本 | V2 成功 / 样本 |
|---|---:|---:|---:|---:|---:|
| 500mb | 1 | 101.5 | 128.2 | 10/10 | 10/10 |
| 500mb | 5 | 83.3 | 118.2 | 20/20 | 20/20 |
| 500mb | 10 | 95.8 | 108.6 | 20/20 | 20/20 |
| 500mb | 50 | 76.2 | 104.3 | 49/50 | 50/50 |
| 1gb | 1 | 68.6 | 106.3 | 10/10 | 10/10 |
| 1gb | 5 | 75.7 | 105.8 | 20/20 | 20/20 |
| 1gb | 10 | 72.5 | 107.8 | 20/20 | 20/20 |
| 1gb | 50 | 73.8 | 108.3 | 50/50 | 50/50 |
| 2gb | 1 | 75.7 | 116.5 | 10/10 | 10/10 |
| 2gb | 5 | 84.2 | 104.2 | 20/20 | 20/20 |
| 2gb | 10 | 70.3 | 105.4 | 20/20 | 20/20 |
| 2gb | 50 | 68.9 | 105.5 | 49/50 | 50/50 |

- **Warm 整体 p50**：V1 为 75.0 ms，V2 为 107.5 ms，增加 43.3%。整体值由所有成功跟测样本合并计算，不是各单元格 p50 的平均值。
- **成功率**：V1 为 298/300（99.33%），V2 为 300/300（100%）；样本量不足以推断长期可靠性提升。
- **错误**：V1 的 500mb/c=50 有 1 次初始化超过 120 秒的 `RuntimeClientError`，2gb/c=50 有 1 次限流；V2 两类错误均为 0。V1 未计入延迟分布的失败等待，使最大成功延迟不等于最坏请求体验。

### 原 V1 报告的启动分类与部署后探测

原报告按进程时间戳将成功样本分成“真实启动”和“预热命中”。其“真实启动”子集汇总如下，p50 沿用原报告的整数毫秒值：

| 镜像 | V1 “真实启动”样本数 | V1 该子集 p50（ms） | V2 同口径底层启动 p50 |
|---|---:|---:|---|
| 500mb | 57 | 8121 | 无法从当前时间戳确认 |
| 1gb | 57 | 11428 | 无法从当前时间戳确认 |
| 2gb | 61 | 13503 | 无法从当前时间戳确认 |

V1 的 c=1 样本全部被原规则判为预热命中，c=50 则有约 82%–86% 的尝试被判为“真实启动”，有助于解释其低并发与高并发分布的差别。但该分类仍是启发式，不能直接移植到 V2；尤其不能将上表子集 p50 与 V2 全体 p50 相除后声称是底层启动加速倍数。

原报告另记录了 500mb 在 `UpdateAgentRuntime` 后首次调用耗时 **7,404 ms**；本次 V2 在新建 Runtime 后的 500mb smoke 为 **2,235.4 ms**。二者分别是更新后与新建后的单次探测，准备过程不同，均未混入正式矩阵，也不据此计算版本性能收益。

**对比结论**：本次 V2 在 c=10/50 下的首次调用中位数和长尾低于 V1 历史测试；c=1/5 的中位数及所有单元格的 warm p50 则更高。若要据此决定迁移，应再做同一天、相同镜像 digest、交替多轮突发的 V1/V2 对照，排除日期和容量状态的影响。

## 进程时间戳与冷启动判定的边界

原脚本保留的 `fresh_boots` 字段计算如下：

```text
(request_ts - proc_start_ts) × 1000 < cold_ms
```

它比较进程年龄与客户端 E2E 时长，是一个**进程年龄启发式**，不是平台提供的冷启动标记：

- 新 session 只确保本次客户端未主动复用已有 session，并不能强制清空主机缓存、预备实例或镜像缓存。
- 如果某种实现会恢复包含进程状态的快照，`proc_start_ts` 可能来自快照制作时刻；一次按需恢复也可能被旧规则判为“非 fresh”。**本测试不能据此确认 V2 使用了快照，也不能由 `fresh_boots=0` 推断没有冷启动或全部命中预热池。**
- `proc_start_ts` 较新也不证明发生了完整镜像拉取、microVM 创建；而原始 `wall_start_iso` 在并发屏障之前记录，不能拿它精确替代实际 API 发起时刻。

本次 300 个矩阵样本的旧 `fresh_boots` 均为 0，每种镜像的 100 个新 session 只出现 **2 个不同的 `proc_start_ts` 值**；进程年龄范围分别约为 330–442 秒、441–555 秒、547–657 秒。重复的进程启动时间戳说明不能把这个字段简单当作 V2 的独立实例 ID，更不能据此认定只启动了 2 台 microVM。

因此本报告以完整新 session 首次调用分布为主，不将 V2 样本按该启发式直接命名为“真实启动 / 预热命中”。原始时间戳和旧字段保留供复核。

### 比较限制

历史数据来自 **2026-07-09** 的原 AgentCore Runtime 测试；其部署脚本没有显式传入 `platformVersion`。本报告将其标为“V1（历史）”作为历史基线，不把旧记录当作当天已核验的 V1 控制组。历史 c=1/10/50 在约 06:05–06:12 UTC 运行，c=5 是约 06:27–06:29 UTC 的后续补测；本次顺序、部署后等待和池状态并非完全一致。

同区域、相同工作负载与样本设计提升了可比性，但日期、SDK、服务端实现、资源调度和缓存状态仍是混杂因素。百分比表示**相对历史观测值的差异**，不单独证明 V2 的因果性能收益。c=50 只有一轮突发，50 个请求并不等于 50 次独立容量实验；不能据此宣称性能 SLA 或镜像大小在所有负载下均无影响。汇总数据中 c=50 占一半样本，也不代表实际业务的并发分布。

历史延迟统计排除了 2 个失败样本，其中一个是初始化超过 120 秒的 `RuntimeClientError`，另一个是限流；失败记录没有 `cold_ms`。所以历史最大成功延迟并不是所有请求的最坏等待时间。

## 可复现性、验证与资源清理

在 `21-runtime-v2-beta/` 下执行：

```bash
# 会创建临时云资源并产生实际调用费用；输出目录必须不存在。
.venv/bin/python -u coldstart_v2.py --out results/coldstart_new_run

# 只读取本地文件，不调用 AWS；使用标准库独立重算分位数并检查完整性。
.venv/bin/python verify_coldstart.py results/coldstart_2026-09-11
```

本次数据目录：[`coldstart_2026-09-11/`](coldstart_2026-09-11/)。

| 文件 | 内容 |
|---|---|
| `raw/*.json` | 每个镜像 × 并发单元格的原始探针，包含 session ID、首次/warm 延迟、时间戳、错误与停止结果 |
| `summary.json` | 原统计函数生成的 12 单元格汇总，保留旧 `fresh_boots` 字段但不将其解读为已证实的 V2 microVM 启动 |
| `responses.jsonl` | 计时结束后保存的完整首次/warm 响应，供核验 HTTP 200、`pong`、echo 和同 session 进程时间戳 |
| `smoke_*.json` | 3 个部署后 smoke 样本，不计入矩阵 |
| `deployments.json` | 本次临时 Runtime ARN/ID、Create 与 READY 响应、V2 回显、镜像与角色信息 |
| `images.json` | 本次部署前 ECR digest、大小、推送时间 |
| `run.json` / `run.log` | SDK 版本、运行时间、测试参数、源码/历史清单 SHA-256 与执行输出 |
| `cleanup.json` | 仅针对本次创建的 Runtime 的删除请求及不存在确认 |

`verify_coldstart.py` 不依赖压测脚本的分位数实现，而是用 `statistics.median` / `statistics.quantiles(method="inclusive")` 重算；检查每档轮次和样本数、300 个唯一矩阵 session、smoke 隔离、完整响应、同 session warm 配对、停止成功、源码 hash 和清理记录。也会从历史原始 JSON 重算并核对历史 summary。

**本次核验结果：全部通过。** 独立核验确认 12 个单元格、300 个唯一矩阵 session、3 个额外 smoke、606 条有效首次/warm 响应；历史与本次统计均匹配原始数据。运行结束后又单独调用 AWS 核对，3 个临时 Runtime 均返回 `ResourceNotFoundException`，三档 ECR 标签的 digest 与部署前一致。未删除原 ECR 镜像、IAM 角色或历史测试 Runtime；自动产生的 CloudWatch 日志未清理，保留用于排查。

适配器是本次受监督实验的运行脚本，不是完善的无人值守资源管理器：强制终止、创建响应丢失或本地磁盘写入失败时仍可能需要人工按唯一 Runtime 名称核对和清理。它沿用原测试的镜像标签 URI；部署前记录 digest 并不等于 API 按 digest 锁定，因此运行期间不要移动标签。原脚本的正常退出本身也不足以证明 300 个样本齐全，必须执行独立核验。
