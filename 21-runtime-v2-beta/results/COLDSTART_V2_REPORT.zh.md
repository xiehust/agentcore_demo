# AgentCore Runtime V2 多进程冷启动测试报告

## TL;DR

**本次多进程测试中，成功被接纳的新 session 首调用延迟总体较稳定，没有随镜像大小或并发档位升高而明显增长。** 在 500mb、1gb、2gb 镜像及 1/10/50/100/200 并发矩阵中，有成功样本的各组首调用中位数／单次值集中在 **2.4–2.9 秒**，并发组的 p90 约 **2.6–3.1 秒**，Warm 中位数约 **0.1 秒**。但“成功请求的典型延迟稳定”不等于“所有请求都能稳定启动”：1083 次尝试中有 **145 次（13.4%）因新 session 创建速率限制被拒绝**，最大成功延迟达到 **5.82 秒**。成功首调用合并 p99 为 **3.367 秒**，但 500mb/c100 的 p99 达 **5.39 秒**，仍有明显长尾。因此，当前结果支持 **V2 在此次轻量 ping-pong 负载下具有较稳定的成功首调用延迟，扩展时需重点关注接纳速率限制**；由于每个组合仅一轮、存在限流筛选和客户端发起跨度，尚不能据此保证长期稳定性或性能 SLA。

**2026-09-13 长尾重复测试更新**：针对 500mb/c100、2gb/c200 各新增三轮。500mb/c100 的 p99 为 **4.349 / 3.503 / 3.044 秒**，263 个新增成功请求无 >5 秒样本；2gb/c200 的 p99 为 **3.479 / 3.519 / 3.467 秒**，566 个新增成功请求中仍有一个 **5.333 秒**长尾。新增 900 次正式尝试中 829 成功、71 限流，六个临时 Runtime 已清理。结论是长尾可偶发复现，并非每轮都有高 p99；CPU/网络/服务端归因仍未确定。新增轮次不混入下方 2026-09-11 矩阵，详见[重复测试报告](TAIL_REPEATS_REPORT.zh.md)。

## 摘要

本报告的完整矩阵使用 **2026-09-11、us-west-2** 的多进程数据：**500mb / 1gb / 2gb × 并发 1 / 10 / 50 / 100 / 200，共 15 格，每格一轮**。其中 12 格新测，500mb 的 c50/c100/c200 复用同日已完成的多进程原始证据，不重复发起请求。

**1083 次正式首调用尝试：938 成功、145 次新 session 创建速率限流，无其他首调用错误。** 成功首调用全部完成同 session warm 跟测；另有 15 次成功 smoke，不混入正式延迟统计。测量和清理均完整，但不是所有请求成功。

- 有成功样本的单元格，首调用 p50／单次值为 **2413.7–2869.7 ms**；Warm p50／单次值为 **101.1–138.2 ms**。它们是各格统计值的范围，不是合并分布。
- **2gb/c1 唯一一次尝试被限流，没有成功延迟可报告**，不记作 0 ms，也不补跑。全部 c1 都只有一个尝试，不能据此估计尾延迟。
- 并发单元格中，只有三个 c10 的 `before-send` 跨度达到 ≤100 ms；全部 c50/c100/c200 未达到。c1 跨度为零只是单请求的必然结果，不构成突发验证。
- 这是**新 session 首次调用 E2E**，不是已确认的 microVM 启动耗时。共享配额、固定执行顺序、不同等待时长及复用批次的时间差，限制了镜像大小和并发扩展性的因果判断。

总清单与核验入口：[`coldstart_v2_matrix_2026-09-11/matrix.json`](coldstart_v2_matrix_2026-09-11/matrix.json)、[`verify_coldstart_v2_matrix.py`](../verify_coldstart_v2_matrix.py)。

## 环境与测量方法

- 区域 **us-west-2**，账号 `434444145045`；私有 boto3 / botocore **1.43.87**，Python **3.12.3**；新运行记录的客户端 CPU affinity 为 0–7。
- 每格使用独立临时 Runtime，均在 READY 后核验 `platformVersion="V2"`。统一为 **PUBLIC / HTTP**、`idleRuntimeSessionTimeout=60`、`maxLifetime=600`，角色为 `AgentCoreColdstartRole`。
- 工作负载仅为 ping-pong 探针，校验 HTTP 200 / `pong` 和完整响应，不调用 LLM。大镜像通过 padding 增大，应用不读取这些填充内容；不能代表启动时加载大模型或大量业务文件的应用。
- 三档镜像均按 digest URI 固定部署。标签是档位名称，大小使用 **MiB（2²⁰ 字节）**；未压缩大小取已有镜像元数据，ECR 压缩大小取本次清单的 `imageSizeInBytes`。

| 镜像 | 未压缩 MiB | ECR 压缩 MiB | 固定 digest |
|---|---:|---:|---|
| 500mb | 500.63 | 382.62 | `sha256:8fce75a892c741d4712f9c3bdd2d0a8f429e69bebdd0393ea96c82f461029d87` |
| 1gb | 1024.63 | 906.78 | `sha256:a2231b90c973736e140e630f608af52812e6d037caa652793f672c77aad8c233` |
| 2gb | 1950.63 | 1833.06 | `sha256:4f3ae6ceae8dfae54b04a56288a5046c4bb52be5854c995e5b17b923f99f83a3` |

### 多进程与样本口径

1. 每格只运行一轮，正式首调用尝试数等于并发 `c`。工作进程数为 **`min(8, c)`**：c1 使用一个 `spawn` 子进程、一个样本；c10/c50/c100/c200 均使用 8 个 `spawn` 工作进程。全矩阵共 **99 次工作进程启动（新测 75、复用 24）**，不是 99 个同时运行的进程。
2. 每个进程独立创建 boto3 客户端、连接池和线程池；线程总数为 c，连接池配置总容量为 **2×c**。父进程预先分配请求索引和唯一 session ID，所有工作线程通过跨进程全局屏障一起释放；不在工作进程间共享 boto3 客户端。
3. SDK **`total_max_attempts=1`**，不重试、不补样。成功首调用后立即在同 session 发起 warm，再停止该 session；限流的 session 也尝试停止。Warm/stop 按 session 独立推进，可与其他首调用重叠，不是另一次同步 warm 突发或稳态吞吐测试。
4. **首调用 E2E** 从该请求调用 SDK 前计时，到完整读取响应体结束；部署、等待 READY、smoke、静置及屏障前等待不计入。响应/错误事件先保存在进程内存，所有工作进程完成流量阶段后才汇集并持久化。
5. [`matrix_multiprocess_client.py`](../matrix_multiprocess_client.py) 复用已冻结的工作客户端，在流量完成后、持久化前明确为结果行赋镜像标签；不更改延迟、响应或错误。现有工作客户端源码 hash 保持不变，清单记录源码及复用证据的 SHA-256。
6. 延迟只统计成功样本，失败计入尝试分母并单列。报告分位数统一从 `raw.json` 中保留到 0.1 ms 的逐请求延迟按线性插值计算（`statistics.quantiles(..., method="inclusive")`），最终保留一位小数。直接从全精度事件计算可能有 0.1 ms 的舍入差异。c1 的成功值只是单次观测；不报告其 p90/p95/p99。没有成功样本时使用 **N/A**。

## 完整结果矩阵

延迟单位均为 **ms**；首调用和 warm 延迟仅对应成功首调用的会话。**N/A 表示无成功样本，或单样本不报告尾分位数**。复用格以“复用”明确标出，其余为新测。

| 镜像 | 并发 | 来源 | 成功 / 尝试 | HTTP 429 | 首调用 p50／单次 | 首调用 p90 | 首调用 p99 | 首调用 max | Warm p50／单次 | Warm p99 |
|---|---:|---|---:|---:|---:|---:|---:|---:|---:|---:|
| 500mb | 1 | 新测 | 1/1 | 0 | 2615.7 | N/A | N/A | 2615.7 | 136.8 | N/A |
| 500mb | 10 | 新测 | 10/10 | 0 | 2611.4 | 2870.0 | 3051.9 | 3072.1 | 123.6 | 147.4 |
| 500mb | 50 | 复用 | 50/50 | 0 | 2533.2 | 2895.9 | 3025.5 | 3045.5 | 101.1 | 152.1 |
| 500mb | 100 | 复用 | 99/100 | 1 | 2429.9 | 2843.2 | 5385.9 | 5524.6 | 105.9 | 216.8 |
| 500mb | 200 | 复用 | 182/200 | 18 | 2561.1 | 3083.3 | 3394.8 | 3647.4 | 103.9 | 172.0 |
| 1gb | 1 | 新测 | 1/1 | 0 | 2869.7 | N/A | N/A | 2869.7 | 138.2 | N/A |
| 1gb | 10 | 新测 | 10/10 | 0 | 2499.2 | 2870.5 | 3238.1 | 3278.9 | 121.9 | 135.2 |
| 1gb | 50 | 新测 | 50/50 | 0 | 2547.6 | 2879.6 | 3200.6 | 3366.0 | 111.0 | 154.7 |
| 1gb | 100 | 新测 | 100/100 | 0 | 2413.7 | 2835.6 | 3225.4 | 3281.6 | 102.5 | 161.0 |
| 1gb | 200 | 新测 | 176/200 | 24 | 2464.6 | 2831.1 | 3225.4 | 3500.7 | 101.8 | 153.0 |
| 2gb | 1 | 新测 | 0/1 | 1 | N/A | N/A | N/A | N/A | N/A | N/A |
| 2gb | 10 | 新测 | 5/10 | 5 | 2572.6 | 2576.9 | 2578.3 | 2578.5 | 104.8 | 130.0 |
| 2gb | 50 | 新测 | 26/50 | 24 | 2678.0 | 3072.3 | 3180.1 | 3195.0 | 107.6 | 138.2 |
| 2gb | 100 | 新测 | 80/100 | 20 | 2627.4 | 2976.3 | 3211.1 | 3215.0 | 101.2 | 155.1 |
| 2gb | 200 | 新测 | 148/200 | 52 | 2517.9 | 3031.4 | 3327.6 | 5822.3 | 101.2 | 145.2 |

### p99 与尾延迟

多数组合的成功首调用 **p99 约为 3.0–3.4 秒**，但 **500mb/c100 的 p99 达到 5385.9 ms（约 5.39 秒）**。这说明典型延迟较稳定，不代表尾延迟也完全稳定。

将全矩阵 **938 个成功正式首调用**合并后，首调用 **p99 为 3367.4 ms（约 3.367 秒）**；对应 938 次 Warm 的 **p99 为 170.9 ms**。合并值受各组合成功样本量影响，不是各格 p99 的平均值，也不代表每个组合都达到该水平。

以上 p99 **排除 15 个 smoke 和 145 次限流失败**，只描述成功请求的尾延迟，不代表全部尝试的完成时间或可靠性。c1 不报告 p99；c10 等小样本格，尤其只有 5 个成功样本的 2gb/c10，其 p99 几乎由最大值决定，估计很不稳定，只能作描述性参考，不能作为长期 SLA。

#### 较高 p99 和最大延迟是否受客户端 CPU 影响

本次测试中，500mb/c100 的 p99 为 **5.39 秒**，2gb/c200 的最大成功延迟为 **5.82 秒**，说明典型延迟较稳定的同时，仍存在少量长尾请求。分段记录显示，两组最慢请求从进入 SDK 到发送前钩子仅耗时约 **2.04 ms 和 0.66 ms**，读取剩余响应体均不足 **0.2 ms**，主要耗时集中在发送前钩子至响应返回之间。该区间同时包含连接获取、TCP/TLS、网络、AgentCore 处理及客户端响应调度，不能直接视为纯服务端启动耗时。对应测试窗口内，全部工作进程的平均 CPU 使用量约为 **0.88 核和 1.69 核当量**，未显示这些工作进程持续耗尽客户端 8 核的迹象；但平均值无法排除短时 CPU 竞争、同机其他负载或调度等待。因此，目前既不能将这些高值归因于客户端核数不足，也不能确认是 AgentCore 服务端瓶颈。该矩阵中各组合仅测试一轮，p99/max 对少量慢请求较敏感。下节给出后续三轮重复测试结果；进一步定位仍需要更细粒度的 CPU、网络及服务端跟踪。

这里的核当量按各 worker 已记录的 `cpu_seconds` 之和，除以最早 `client_ready_perf` 到最晚 `finished_perf` 的窗口跨度计算，包含线程准备、首调用、warm 和停止等过程，不是整机或逐核的瞬时利用率。分段分析选取的是每组最大延迟的实际请求，并非为插值得到的 p99 指认某个具体请求；`before-send` 与 `response-received` 也只是 SDK 观测点，不是精确的网络发包或服务端执行边界。

#### 三轮重复测试：长尾是否稳定复现（2026-09-13）

针对 500mb/c100 和 2gb/c200 各新增三轮多进程测试，使用相同镜像 digest、区域、角色及 Runtime 配置，每轮独立创建临时 V2 Runtime 和 8 个工作进程。两个组合交替执行；相邻正式流量之间至少保持 180 秒无本测试目标调用，不自动重试、不调整配额。以下延迟单位为 **秒**，只统计成功首调用；从原始事件全精度 `elapsed_ms` 做 inclusive 线性插值，再换算并保留三位小数。

| 组合 | 新增轮次 | 成功 / 尝试 | HTTP 429 | p99 | max | >5 秒成功请求数 |
|---|---:|---:|---:|---:|---:|---:|
| 500mb/c100 | 1 | 100/100 | 0 | 4.349 | 4.894 | 0 |
| 500mb/c100 | 2 | 82/100 | 18 | 3.503 | 3.715 | 0 |
| 500mb/c100 | 3 | 81/100 | 19 | 3.044 | 3.076 | 0 |
| 2gb/c200 | 1 | 183/200 | 17 | 3.479 | 5.333 | 1 |
| 2gb/c200 | 2 | 192/200 | 8 | 3.519 | 3.602 | 0 |
| 2gb/c200 | 3 | 191/200 | 9 | 3.467 | 3.833 | 0 |

**更新后的判断：长尾可以偶发复现，但不是每轮都出现约 5 秒的 p99。**

- **500mb/c100**：原 p99 5.39 秒的程度未在三轮新增测试中再次出现。仅合并新增 263 个成功请求，p99 为 **4.019 秒**，max 为 **4.894 秒**；3 个请求超过 4 秒，没有超过 5 秒的请求。未观测到不代表真实发生率为零。
- **2gb/c200**：三轮 p99 都约 **3.5 秒**，但第一轮仍有一个 **5.333 秒**请求；仅合并新增 566 个成功请求，p99 为 **3.518 秒**。因此，p99 较稳定不排除极少量慢请求，仍需同时看 max 和阈值计数。
- **根因仍未确定**：新增四个超过 4 秒的成功请求，调用开始至发送前钩子均不足 1 ms，响应钩子至读完 body 均不足 0.2 ms，耗时主要仍在 SDK 两个钩子之间。六轮 worker 平均 CPU 核当量约 0.92–2.68，没有显示持续耗尽 8 核；但未新增逐核 profiling、网络抓包或服务端 tracing，不能据此排除瞬时 CPU 竞争或确认服务端瓶颈。

新增共 **829/900 成功、71 次限流**，829 次正式 warm 均成功；含六个 smoke，835 次停止返回 HTTP 200，71 个限流会话的停止返回未找到。六个临时 Runtime 已确认删除。新增轮次不与上方 2026-09-11 矩阵合并，也不对各轮 p99 取平均；原两组是因高值被选中，日期、等待条件和成功接纳数也不同，不能将新增数值下降解释为平台改善。

每个组合仍只有三次新增突发，发送前跨度均未达到 ≤100 ms 目标；180 秒间隔只约束本测试流量，不保证账号其他流量消失或配额恢复。完整逐轮统计、SDK 分段数据、CPU 计算及证据链接见[长尾重复测试报告](TAIL_REPEATS_REPORT.zh.md)。

### 样本与调用结果核对

| 来源 | 单元格 | 正式尝试 | 成功 | 限流 | 其他首调用错误 | Warm 成功 / 实际跟测 |
|---|---:|---:|---:|---:|---:|---:|
| 新测 | 12 | 733 | 607 | 126 | 0 | 607/607 |
| 复用 500mb c50/c100/c200 | 3 | 350 | 331 | 19 | 0 | 331/331 |
| 合计 | 15 | 1083 | 938 | 145 | 0 | 938/938 |

每格另有一个 smoke（首调用、warm、stop），共 **15/15 成功**，不是正式样本。正式首调用成功率为 **938/1083（86.61%）**，限流率为 **145/1083（13.39%）**；不要把“有完整记录”写成“全部调用成功”。

## 执行时间线与等待条件

所有时间均为 **2026-09-11 UTC**。复用三格的正式流量窗口为 **07:34:43–07:47:30**；新运行从 **08:15:32** 开始，到 **08:26:40** 完成，12 格正式流量窗口为 **08:22:49–08:26:25**。正式流量窗口包括各 session 的首调用、warm 和 stop，不包括 Runtime 部署。

新运行先创建全部 12 个 Runtime，再依次等 READY 并串行执行各格 smoke；**最后一个 smoke 停止后统一等待 180 秒**，然后串行运行正式单元格。固定顺序如下：

```text
500mb: c1 → c10
1gb:   c1 → c10 → c50 → c100 → c200
2gb:   c1 → c10 → c50 → c100 → c200
```

每个新测单元格完成后删除对应 Runtime，再暂停 10 秒。该流程避免本运行的正式突发彼此重叠，但**不保证账号共享配额恢复到相同状态**，也不代表所有单元格恰好静置 180 秒。

独立 verifier 以各格 smoke 的 stop 完成时间到第一条正式 SDK 调用开始时间重算等待：复用三格为 **180.275–180.403 秒**，新测 12 格为 **248.742–388.371 秒**。新测后面的格还经历前面格的流量与清理，不能把“本 Runtime 距 smoke 的等待”当作“全账号没有流量的恢复时间”。清单中的 `observed_settle_seconds` 在启动工作进程前记录，因此略小于此处的实际首调用等待。

## 实际发起跨度与并发含义

下表由全部首调用事件（含限流）重算。**SDK 起点跨度**是最后与最早 SDK 调用前计时点之差；**before-send 跨度**是 botocore `before-send` hook 最晚与最早触发时间之差。该 hook 位于传输前，**不是 socket 实际写出、线上发包或服务端到达时间**。峰值重叠只统计客户端首调用计时区间，不是活跃 session 或 microVM 数。

| 镜像 | 并发 | 工作进程 | SDK 起点跨度 ms | before-send 跨度 ms | ≤100 ms 目标 | 首调用区间峰值重叠 |
|---|---:|---:|---:|---:|---|---:|
| 500mb | 1 | 1 | 0.000 | 0.000 | 不适用（单请求） | 1 |
| 500mb | 10 | 8 | 3.315 | 3.424 | 达到 | 10 |
| 500mb | 50 | 8 | 264.943 | 263.771 | 未达到 | 50 |
| 500mb | 100 | 8 | 541.260 | 540.058 | 未达到 | 100 |
| 500mb | 200 | 8 | 1224.435 | 1223.288 | 未达到 | 194 |
| 1gb | 1 | 1 | 0.000 | 0.000 | 不适用（单请求） | 1 |
| 1gb | 10 | 8 | 4.053 | 2.506 | 达到 | 10 |
| 1gb | 50 | 8 | 184.787 | 183.573 | 未达到 | 50 |
| 1gb | 100 | 8 | 503.176 | 501.910 | 未达到 | 100 |
| 1gb | 200 | 8 | 1106.118 | 1105.002 | 未达到 | 185 |
| 2gb | 1 | 1 | 0.000 | 0.000 | 不适用（单请求） | 1 |
| 2gb | 10 | 8 | 8.014 | 6.798 | 达到 | 10 |
| 2gb | 50 | 8 | 215.101 | 214.010 | 未达到 | 48 |
| 2gb | 100 | 8 | 583.556 | 581.946 | 未达到 | 88 |
| 2gb | 200 | 8 | 1238.363 | 1261.200 | 未达到 | 172 |

**12 个并发单元格中仅 3 个达到发送前 ≤100 ms 目标，均为 c10；其余 9 个均未达到。** verifier 对 c1 的布尔比较也会得到 true，但零跨度没有突发含义，本报告明确记为不适用。c200 的 before-send 跨度约 1.11–1.26 秒，因此不能把 c200 描述为 200 个请求在 100 ms 内同时到达服务端。

每个请求从自己的 SDK 调用前计时，不能把全格发起跨度直接从该请求 E2E 中扣除。没有网络抓包或服务端分段计时，本报告不指定调度、连接建立或服务端处理中的某一项为根因。

## 限流解释与结果边界

145 次正式失败全部为 **`ThrottlingException` / HTTP 429 / `New session creation rate exceeded`**。清单记录本次账号、区域配额如下；没有调整配额：

| 配额 | 记录值 |
|---|---:|
| 新 Runtime session 创建速率（L-8EE2AEA2） | 25 TPS |
| Runtime 数据面 API 速率（L-46ED137C） | 1000 TPS |
| 账号活跃 session 数（L-3E5722B2） | 5000 |

错误明确指向**新 session 创建速率**，不是镜像启动错误，也不是达到活跃 session 容量上限的证据。配额由账号共享；一次突发接纳超过 25 个请求，不能解释为持续 TPS 配额提高。成功数也不能用于推断最大并发容量。

特别是 **2gb/c1**，它在固定顺序中紧接 **1gb/c200**（沿用删除和 10 秒暂停流程），唯一首调用得到 429，未重试。该格测量完整但无成功延迟；不能将其写成“2gb 单请求也无法启动”，或把原因归于镜像大小。前轮消耗、突发额度和账号其他流量未被隔离，不能假定它是在完全恢复的配额状态下运行。

成功分布需和成功分母一起阅读：例如 2gb/c200 的 p50 为 2517.9 ms，但仅 **148/200** 成功；它不是 200 个请求都被接纳时的延迟分布。错误等待时间保留在原始事件中，不混入成功延迟，也不能用成功 max 代表所有失败请求的最坏等待。

## 本轮结论与适用范围

1. **完整覆盖了指定 15 格，但未实现全请求成功。** 正式 938/1083 成功、145 次新 session 速率限流。成功会话的 warm 跟测均通过；2gb/c1 只得到一次限流结果，没有可用的成功首调用基线。
2. **成功首调用中位数集中在约 2.4–2.9 秒，但不是通用性能保证。** 本轮同并发下的成功 p50 并未随镜像大小一致递增；这不能证明大镜像无成本，也不能从受限流筛选的成功分布推出扩展能力。应用不读取 padding，实际初始化负载很轻。
3. **高并发不是理想同时到达的流量。** 三档 c200 均出现限流，而且全部 c50 以上未达到 ≤100 ms 发送前跨度目标。进程隔离和全局屏障不等于网络或服务端同时接收。
4. **每格只有一轮，且准备条件并非完全等价。** c1 仅一个尝试；c10 尾分位数也只有少量成功样本支撑。固定顺序、账号共享配额、复用三格与新测的时间差、不同 smoke 等待时长，以及无法观测的缓存和调度状态，均为混杂因素。大量同轮请求不等于多次独立实验，不能据此建立 SLA、稳定吞吐或确定的并发性能曲线。

### “冷启动”命名的边界

本报告中的冷启动数据严格指**全新 session 的首次调用 E2E**。新 session ID 不会强制清空平台缓存，也不能证明发生了完整镜像拉取、microVM 创建或某种特定恢复流程。

响应中的进程时间戳及证据中的 `fresh_boots` 是应用层信息／启发式，不是平台提供的启动证明。一般而言，若系统恢复包含进程状态的快照，时间戳可能保留较早值；**这只是测量方法的通用限制，不是本实验确认了快照机制或认定它导致某项结果**。因此不按这些字段给请求贴“真实启动／预热命中”标签，也不从时间戳重复推断实例数量。

本轮没有追加请求来消除这些限制，也没有为获得全绿结果重试或更改配额。

## 证据索引

以 [`matrix.json`](coldstart_v2_matrix_2026-09-11/matrix.json) 的 `cells[*].folder` 为准定位原始数据；下面逐格链接与清单一致。新测证据位于 [`coldstart_v2_matrix_2026-09-11/`](coldstart_v2_matrix_2026-09-11/)，复用证据保持在 [`coldstart_v2_multiprocess_500mb_2026-09-11/`](coldstart_v2_multiprocess_500mb_2026-09-11/)，不复制成新测结果。

| 镜像 | c1 | c10 | c50 | c100 | c200 |
|---|---|---|---|---|---|
| 500mb | [新测](coldstart_v2_matrix_2026-09-11/500mb_c1/) | [新测](coldstart_v2_matrix_2026-09-11/500mb_c10/) | [复用](coldstart_v2_multiprocess_500mb_2026-09-11/c50/) | [复用](coldstart_v2_multiprocess_500mb_2026-09-11/c100/) | [复用](coldstart_v2_multiprocess_500mb_2026-09-11/c200/) |
| 1gb | [新测](coldstart_v2_matrix_2026-09-11/1gb_c1/) | [新测](coldstart_v2_matrix_2026-09-11/1gb_c10/) | [新测](coldstart_v2_matrix_2026-09-11/1gb_c50/) | [新测](coldstart_v2_matrix_2026-09-11/1gb_c100/) | [新测](coldstart_v2_matrix_2026-09-11/1gb_c200/) |
| 2gb | [新测](coldstart_v2_matrix_2026-09-11/2gb_c1/) | [新测](coldstart_v2_matrix_2026-09-11/2gb_c10/) | [新测](coldstart_v2_matrix_2026-09-11/2gb_c50/) | [新测](coldstart_v2_matrix_2026-09-11/2gb_c100/) | [新测](coldstart_v2_matrix_2026-09-11/2gb_c200/) |

| 文件 | 内容 |
|---|---|
| 总目录 `matrix.json` / `run.log` | 15 格来源、12 个创建请求、Runtime 响应、镜像 digest、配额、源码和复用文件 hash、状态及日志 |
| 各格 `client_plan.json` | 父进程计划、session ID、进程分配、连接池容量和发送前跨度目标 |
| 各格 `client_result.json` | 各工作进程报告、PID、计时、事件、退出码及屏障释放点 |
| 各格 `raw.json` | 正式请求行、完整响应／错误事件、首调用／warm／stop 配对 |
| 各格 `summary.json` | 该格统计和成功／限流／停止计数 |
| 各格 `smoke.json` / `cleanup.json` | 不计入正式矩阵的 smoke，以及该 Runtime 删除与不存在确认 |

## 核验、清理与复现入口

在项目根目录 `/home/ubuntu/workspace/agentcore_demo/21-runtime-v2-beta` 执行以下**离线**命令，不调用 AWS：

```bash
.venv/bin/python -B verify_coldstart_v2_matrix.py results/coldstart_v2_matrix_2026-09-11
PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -B -m unittest test_matrix_multiprocess test_multiprocess_coldstart -v
```

独立 verifier **通过**：核对全部 15 格、1083 个正式尝试、15 个 smoke、**1098 个唯一 session ID**，重算分位数和发起跨度，检查索引与计划、独立进程及连接池、无重试配置、完整响应、warm/stop 配对、V2 与镜像配置、源码 hash、3 格复用证据 hash 和删除记录。离线测试共 **9 项通过**，包括真实 spawn 的低／高并发 mock、镜像标签、精确复用范围、初始化失败、崩溃／挂起进程回收及计时 hook 错误路径。

### 停止与 Runtime 清理

- 正式成功会话 **938 次**、smoke **15 次**，合计 **953 次 Stop 返回成功**。
- 限流会话仍发起 **145 次 Stop**，全部返回 `ResourceNotFoundException`；这表示停止时未找到，不计为成功停止，也不能证明它们从未短暂存在。
- 新运行只负责其创建的 **12 个 Runtime** 删除；3 格复用对应的 Runtime 已在该批次清理，不再次删除。清单逐格删除记录完整；执行方另于 **08:26 UTC** 对全部 **15 个 Runtime** 实时只读复查，均已不存在。复查没有修改任何资源。
- `measurement_complete=true`、`cleanup_complete=true`、`all_invocations_successful=false`、**`exit_code=1`** 原样保留。退出码 1 与限流及其后未找到的停止结果一致，不代表矩阵没跑完；verifier 通过证明证据完整，不把 429 改写为成功。
- 原始证据、其他报告、镜像及 IAM 角色均保留；没有因重写文档删除任何旧文件。Runtime 删除不等于自动清除相关日志或其他持久化资源。

### 另一次明确获准的实测

[`coldstart_v2_matrix.py`](../coldstart_v2_matrix.py) 是该矩阵入口：**新建 12 格、引用 3 格已存在的 500mb 多进程证据**，不是把 15 格全部重跑。必须先取得明确的云资源与费用授权；程序没有交互式授权提示。输出目录必须在本项目内且此前不存在，不可覆盖本报告证据。

```bash
# 仅供另一次明确批准后执行；会调用 AWS、创建 12 个临时 Runtime 并产生费用
.venv/bin/python -B -u coldstart_v2_matrix.py --out results/coldstart_v2_matrix_NEW_RUN

# 该次运行结束后仍需独立核验
.venv/bin/python -B verify_coldstart_v2_matrix.py results/coldstart_v2_matrix_NEW_RUN
```

上述 `NEW_RUN` 命令仅为后续复现示例，不会因读取报告而执行。强制终止或创建响应丢失等异常仍需按运行清单核对资源，不能仅凭进程退出推定已完整清理。
